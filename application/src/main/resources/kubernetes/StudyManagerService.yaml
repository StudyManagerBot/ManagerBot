apiVersion: v1
kind: Service
metadata:
  name: study-manager-svc
  namespace: YOUR_NAME_SPACE
spec:
  ports:
    - name: study-manager-svc
      port: 80
      targetPort: 80
  selector:
    app: study-manager
  type: ClusterIP